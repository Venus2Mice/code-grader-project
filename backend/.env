# Backend Environment Variables
# Used when Flask app runs in Docker container

# Database connection (use service name 'postgres' when in Docker)
DATABASE_URL=postgresql://postgres:postgres123@postgres:5432/code_grader

# Flask configuration
SECRET_KEY=backend-secret-key-change-in-production-67adsads78JKO9dH
FLASK_APP=run.py
FLASK_DEBUG=1

# RabbitMQ connection (use service name 'rabbitmq' when in Docker)
RABBITMQ_HOST=rabbitmq
